# Skill Tree — runtime pip dependencies (what the app needs to run).
# Mirrors environment.yml; keep the two in sync when bumping a version.
# Test and packaging tools live in requirements-dev.txt.
# Conda users should prefer environment.yml instead.
# Requires Python 3.13.

dash==4.1.0
dash-bootstrap-components==2.0.4
dash-cytoscape==1.0.2
networkx==3.4.2
numpy==2.2.6
plotly==6.6.0

# The Electron desktop shell needs Node.js (a conda dependency — see
# environment.yml) plus its npm packages; see setup.md. Node is not
# pip-installable, so there's nothing to add here for it.
