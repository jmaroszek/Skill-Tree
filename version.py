"""The application version, the one source for Python, the logs and the shell.

electron/package.json carries the same string, and a test keeps the two equal,
so an installer, its update feed and the server it runs always agree.
"""

__version__ = "0.9.0"
